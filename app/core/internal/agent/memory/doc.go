// Package memory owns the boundary for durable, cross-conversation agent memory.
//
// Conversation history and runtime context construction belong to the
// conversation and runtime packages respectively. Long-term memory behavior
// is not implemented until its storage and retrieval semantics are defined.
package memory
